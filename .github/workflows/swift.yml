name: Swift
on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev
jobs:
  build:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: exec ./.github/build.sh
      - name: Test
        run: exec ./.github/test.sh
